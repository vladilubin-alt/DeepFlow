# DeepFlow ADHD Writing Timer Constitution

## Global Identity
DeepFlow is an ADHD-focused writing timer designed to help users enter and maintain flow states. It uses deterministic behavioral psychology triggers (like the "Forgiving Guillotine" and auditory/visual ADHD flares) coupled with an Apple-esque minimalistic UI.

## Behavioral Rules
- **Apple-esque Minimalistic UI**: High aesthetic priority, clean layouts, custom typography, subtle micro-animations. Avoid typical loud or gamified "AI" tropes.
- **ADHD-Centric Flow**: Design features must reduce cognitive friction and prevent task freeze.
- **Shame Reduction**: Implementation of "Grace Tokens" and forgiveness mechanics to prevent users from deleting the app out of guilt when a streak is broken.

## Architectural Invariants
- **Deterministic Business Logic**: Timer state, guillotine logic, penalty calculations, and session logging must be 100% deterministic and validated on the backend.
- **Probabilistic UI**: UI transitions, feedback loops, and decorative elements can be dynamic/probabilistic to maintain high novelty.
- **Capacitor Build Rules**:
  - Safe-area insets: Use `env(safe-area-inset-top)` and `env(safe-area-inset-bottom)` on all full-width containers to avoid notches and home indicators.
  - Status bar: Configure `StatusBar.setOverlaysWebView({ overlay: false })` on app init to prevent content from rendering under the status bar.
  - Splash screen: Use `@capacitor/splash-screen` plugin with Midnight Luxe background color (`#0D0D12`) — no logo, just a clean fade.
  - Keyboard handling: `Keyboard.setResizeMode('body')` on Android to prevent the keyboard from compressing the viewport.
  - File access: Capacitor WebView uses `http://localhost` — no CORS issues for Supabase REST calls.
  - Deep links (future): Register custom URL scheme `deepflow://` for OAuth callback redirects.

## Security Policy (80/20 Rule)
- **Row Level Security (RLS)**: Enforced on all tables ("User A can only see User A's data").
- **Secret Management**: API keys and secrets strictly referenced via `.env` imports only.
- **Server-Side Validation**: All user input, emails, and session data validated on the server/database functions.
- **Dependency Checks**: Periodic audit of third-party packages.

## Data Schemas

### 1. User Profiles (`profiles`)
Stores user-specific stats, streak counts, and available Grace Tokens.
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "UserProfile",
  "type": "object",
  "properties": {
    "id": { "type": "string", "format": "uuid", "description": "References auth.users(id)" },
    "username": { "type": ["string", "null"] },
    "streak_count": { "type": "integer", "minimum": 0, "default": 0 },
    "grace_tokens": { "type": "integer", "minimum": 0, "default": 3 },
    "last_active_at": { "type": ["string", "null"], "format": "date-time" },
    "created_at": { "type": "string", "format": "date-time" },
    "updated_at": { "type": "string", "format": "date-time" }
  },
  "required": ["id", "streak_count", "grace_tokens", "created_at", "updated_at"]
}
```

### 2. Writing Sessions (`writing_sessions`)
Logs each writing session, timer metrics, and guillotine/grace token outcomes.
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "WritingSession",
  "type": "object",
  "properties": {
    "id": { "type": "string", "format": "uuid" },
    "user_id": { "type": "string", "format": "uuid", "description": "References profiles(id)" },
    "started_at": { "type": "string", "format": "date-time" },
    "ended_at": { "type": ["string", "null"], "format": "date-time" },
    "duration_seconds": { "type": "integer", "minimum": 0 },
    "target_words": { "type": "integer", "minimum": 0 },
    "words_written": { "type": "integer", "minimum": 0 },
    "guillotine_triggered": { "type": "boolean", "default": false },
    "grace_token_used": { "type": "boolean", "default": false },
    "status": { 
      "type": "string", 
      "enum": ["active", "completed", "guillotined", "saved_by_grace"] 
    },
    "created_at": { "type": "string", "format": "date-time" }
  },
  "required": ["id", "user_id", "started_at", "duration_seconds", "target_words", "words_written", "guillotine_triggered", "grace_token_used", "status"]
}
```

### 3. Drafts (`drafts`)
Stores primary text data to prevent catastrophic loss when crashes occur.
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Draft",
  "type": "object",
  "properties": {
    "id": { "type": "string", "format": "uuid" },
    "session_id": { "type": "string", "format": "uuid", "description": "References writing_sessions(id)" },
    "user_id": { "type": "string", "format": "uuid", "description": "References profiles(id)" },
    "content": { "type": "string" },
    "word_count": { "type": "integer", "minimum": 0 },
    "created_at": { "type": "string", "format": "date-time" },
    "updated_at": { "type": "string", "format": "date-time" }
  },
  "required": ["id", "session_id", "user_id", "content", "word_count", "created_at", "updated_at"]
}
```

---

## Keystroke Validator — Heuristics Reference

**File:** `src/utils/keystrokeValidator.js`

The validator gates all keystroke events before they reach the session timer. If any heuristic rejects the text, the keystroke is silently dropped and the idle timer is **not** reset — preventing spam from gaming the system.

### Heuristic 1 — Keyboard Walking (Spam Detection)
Detects consecutive characters typed by dragging a finger across a QWERTY keyboard row.
- **Input:** Any word ≥ 5 characters.
- **Method:** Builds an adjacency map from three QWERTY rows (`qwertyuiop`, `asdfghjkl`, `zxcvbnm`). If a sequence of ≥5 characters are all adjacent on the same row, the word is rejected.
- **False Positives:** Common passwords like "qwerty", "asdfgh", "zxcvbn" are correctly rejected.
- **Tuning:** Change `minLength` param (currently 5) in `isKeyboardWalk()` to adjust sensitivity.

### Heuristic 2 — Character Repetition
Detects long runs of the same character (e.g. `aaaaaaa`, `......`, `1111111`).
- **Input:** Any word ≥ 5 characters.
- **Method:** Strip to alphanumeric, count unique characters — if exactly 1, reject.
- **Tuning:** The minimum word length (5) can be increased for leniency with intentional patterns like "nooooo".

### Heuristic 3 — Lexical Structure
Detects gibberish by analyzing vowel/consonant ratios and clusters.
- **No Vowels:** Any word ≥ 4 letters with zero vowels (`aeiouy`) → reject.
- **All Vowels:** Any word > 4 letters with zero consonants → reject.
- **Consonant Cluster:** ≥ 6 consecutive consonants → reject (e.g. `sdfghj`).
- **Vowel Cluster:** ≥ 5 consecutive vowels → reject (e.g. `aeioua`).
- **Tuning:** Adjust threshold constants (6 for consonants, 5 for vowels) or the vowel set (remove `y` for strict mode).

### Boundary Rules
- **Empty/whitespace-only** text → rejected.
- **Extremely long tokens** (> 25 chars after stripping punctuation) → rejected.
- **Only valid keystrokes** pass through to `DeepFlowSession.keystroke()`.

---

## Git & Deployment Infrastructure

### Remote
- **Origin**: `git@github.com:Vladi758/DeepFlow.git` (SSH)
- **SSH Key**: `~/.ssh/github_deploy` (ed25519, `ssh -T git@github.com` verified)
- **Branch**: `main` (single trunk)

### Netlify CI/CD Build Settings
- **Production URL**: `https://gleeful-liger-6f788b.netlify.app`
- **Netlify Admin**: `https://app.netlify.com/projects/gleeful-liger-6f788b`
- **Build Command**: `npm run build` (via `netlify.toml`)
- **Publish Directory**: `dist/`
- **SPA Redirect**: `/* → /index.html` (status 200)
- **Trigger**: Push to `main` → automatic production deploy

### Node Runtime
- **Local**: `.tmp/node-v22.12.0-darwin-x64/bin/node`
- **Package Manager**: npm 10.9.0
- **Build Tool**: Vite 8.0.16

---

## Maintenance Log

| Date | Version | Change | Author |
|------|---------|--------|--------|
| 2026-06-22 | v0.4.0 | Phase 4 initial deploy (Midnight Luxe refinement, GSAP orb, binaural audio, mobile layout) | DeepFlow |
| 2026-06-22 | v0.4.1 | Anonymous Auth Flow: `signInAnonymously()` handshake, `setAuth()` propagation to SyncService | DeepFlow |
| 2026-06-22 | v0.4.1 | Keystroke Validation Gate: ported heuristics from `validate_keystrokes.py` to `src/utils/keystrokeValidator.js` | DeepFlow |
| 2026-06-22 | v0.4.1 | Emergency Recovery SOP: `architecture/emergency_recovery.md` with Graveyard manual access steps | DeepFlow |
| 2026-06-22 | v0.4.1 | Netlify deploy: `netlify.toml` config, anonymous deploy, permanent site at gleeful-liger-6f788b | DeepFlow |
| 2026-06-22 | v0.4.2 | Recovery Vault UI: `VaultModal`, `useGraveyard` hook, shared `supabase.js` client | DeepFlow |
| 2026-06-22 | v0.4.2 | SSH Git remote (ed25519), Netlify-to-GitHub CI/CD linkage | DeepFlow |
| 2026-06-22 | v0.4.2 | Supabase Auth Site URL + Redirect URLs configured for production origin | DeepFlow |
| 2026-06-22 | v0.4.2 | Project Memory sealed — all 5 Phases complete | DeepFlow |
